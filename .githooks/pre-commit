#!/usr/bin/env bash
set -e

echo "🔍 Running backend checks..."
if [ -d backend ]; then
  (cd backend && ./gradlew -q spotlessApply test)
fi

echo "🔍 Running frontend lint..."
if [ -d frontend ]; then
  (cd frontend && npm run -s lint)
fi

echo "✅ All checks passed"
